import styled from "styled-components";

const Header = () => {
    return (
       <Wrapper>
           Random game picker
       </Wrapper>
    )
};

const  Wrapper = styled.header`
    height: 50px;
    text-align:center; 
`;

export default Header;
