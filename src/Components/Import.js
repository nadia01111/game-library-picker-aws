import styled from "styled-components";

const Import = () => {
    return (
       <Wrapper>
           Import from...
       </Wrapper>
    )
};

const  Wrapper = styled.div``;

export default Import;
