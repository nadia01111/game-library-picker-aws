import styled from "styled-components";

const CurrentGame = () => {
    return (
       <Wrapper>
           Current Game
       </Wrapper>
    )
};

const  Wrapper = styled.div``;

export default CurrentGame;
